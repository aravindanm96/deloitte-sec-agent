version: 0.2
phases:
  install:
    commands:
      - python -m pip install --upgrade pip
      - pip install -r requirements.txt
  build:
    commands:
      - echo "No build steps needed for Streamlit"
  post_build:
    commands:
      - echo "Ready to start Streamlit!"
artifacts:
  files:
    - '**/*'
